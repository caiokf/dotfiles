# SSH config - shared across machines
# Machine-specific keys go in ~/.ssh/config.local (not in git)

Include ~/.ssh/config.local

# Common settings for all hosts
Host *
    AddKeysToAgent yes
    IdentitiesOnly yes
